name: Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # - name: Authenticate heroku
      #   run: heroku auth:${{secrets.HEROKU_API_KEY}}
      - name: Push to Heroku
        run: git push https://muriithited:${{secrets.HEROKU_TOKEN}}@git.heroku.com/stock-info-summary.git

      # - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
      #   with:
      #     heroku_api_key: ${{secrets.HEROKU_API_KEY}}
      #     heroku_app_name: "stock-info-summary" #Must be unique in Heroku
      #     heroku_email: "muriithited@gmail.com"
      #     branch: "master"
